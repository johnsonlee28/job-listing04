<!DOCTYPE html>
<html>
<head>

</head>
<body>

 <div class="center hero-unit" style="background:#B3E5FC; padding-top:10px; padding-bottom:15px;">

    <h1>Welcome to the Victor Hunting</h1>

    <h2>
      This is a dreaming stage!We are appreciated to fix up for you!
     <!-- 这是一个为企业和个人展现自我的舞台！我们能为您付出更多来建造！ -->

    </h2>
    <br>

   <%= link_to "Sign up now!", '#', class: "btn btn-large btn-primary" %>

 </div>
<!-- search bar -->



<!-- 职位有关内容 -->
<br>
<div class="dropdown clearfix pull-right">
   <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
     排序
     <span class="caret"></span>
   </button>
   <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
     <li>
       <%= link_to("按照薪资下限排序", jobs_path(:order => "by_lower_bound")) %>
     </li>
     <li>
       <%= link_to("按照薪资上限排序", jobs_path(:order => "by_upper_bound")) %>
     </li>
     <li>
       <%= link_to("按照发表时间排序", jobs_path) %>
     </li>
   </ul>
</div>
<br><br>
<div class="panel panel-default" >
  <!-- Default panel contents -->

  <!-- Table -->
<table class="table table-boldered table-striped custab">
  <thead>
    <tr>
      <td>#</td>
      <td>职位招募</td>
      <td>企业名称</td>
      <td>城市</td>
      <td>薪资范围</td>
      <td>发布时间</td>
    </tr>
  </thead>
  <tbody>
   <% @jobs.each_with_index do |job, index| %>
     <tr>
       <td><%= index +1%></td>
       <td><%= link_to(job.title, job_path(job)) %></td>
       <td><%= job.company_name %></td>
       <td><%= job.place %></td>
       <td width="15%">
         <span class="badge badge-info">
           <span class="fa fa-rmb"></span><%= job.wage_lower_bound %> - <%= job.wage_upper_bound %> 元
         </span>
       </td>
       <td width="10%">
          <%= time_ago_in_words(job.created_at) %> 前
       </td>
     </tr>
  <% end %>
</tbody>
</table>

</div>
<div class="text-center">
  <%= will_paginate @jobs %>
</div>

</body>
</html>
