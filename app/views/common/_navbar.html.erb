<nav class="navbar navbar-default navbar-fixed-top" role="navigation">

    <!-- Brand and toggle get grouped for better mobile display -->
    <!-- <div class="navbar-header" > -->
  <div class="container-fluid">
    <div> <a class="navbar-brand" href="/" style="pull-left" ><img src='<%= image_url 'logo.png' %>'>Victor</a>
     </div>  <div><%= render :partial => "jobs/search_bar" %></div>


    <!-- Collect the nav links,forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

          <ul class="nav navbar-nav navbar-right js-nav">
            <% if !current_user %>
             <li><%= link_to('<span class="glyphicon glyphicon-user"></span> 注册'.html_safe, new_user_registration_path) %></li>
             <li><%= link_to('<span class="glyphicon glyphicon-log-in"></span> 登入'.html_safe, new_user_session_path)%></li>
            <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                 Hi!,<%= current_user.email %>
                 <b class="caret"></b>
               </a>
                <ul class="dropdown-menu">
                 <% if current_user.admin? %>
                   <li><%= link_to("管理员", admin_jobs_path) %></li>
                 <% end %>
                   <li><%= link_to("<span class='glyphicon glyphicon-log-out'></span> 登出".html_safe,destroy_user_session_path, method: :delete) %></li>
               </ul>
             </li>
           <% end %>
         </ul>
        </div>
    <!-- /.navbar-collapse -->
  </div>

  <!-- /.container-fluid -->
</nav>
